# Build and run orchestrator (Python/Node/Go depending on code) - default to a small Python runner
FROM python:3.10-slim
WORKDIR /opt/orchestrator
COPY . .
RUN pip install --no-cache-dir -r requirements.txt || true
EXPOSE 8080
CMD ["python", "main.py"]
